No diretorio do nosso compose, 
baixe as imagens:

docker compose pull

Execute:

docker compose up -d

Apos subir os servicos, acessar o Web:

http://localhost:8080/

Para dar um restart:

docker compose down

docker compose up -d

Aguarde uns segundos para as imagens subirem.